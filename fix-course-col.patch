--- apps/frontend/src/components/Courses/CourseColumn.tsx
+++ apps/frontend/src/components/Courses/CourseColumn.tsx
@@ -272,16 +272,16 @@
         };
 
         return (
-          <Card key={course.id}
-            style={{ backgroundColor: course.color || getColorForString(course.name) }}
-            className="bg-white border-slate-200 shadow-sm overflow-hidden">
+          <Card key={course.id} className="bg-white border-slate-200 shadow-sm">
             <CardHeader
-              className=""
+              className="pb-3 border-b border-slate-100"
+              style={{ backgroundColor: course.color || getColorForString(course.name) }}
             >
               <CardTitle className="text-xl flex justify-between items-center text-slate-900">
                 {course.name}
               </CardTitle>
             </CardHeader>
-            <CardContent className="p-3 rounded">
-              <div className="">
+            <CardContent className="p-0">
+              <div className="divide-y divide-slate-100">
                 {currentIlos.map((ilo: any) => {
